{
  "name": "questline-webapp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/.htaccess dist/.htaccess && mkdir -p dist/api && cp api/index.php api/.htaccess dist/api/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "marked": "^12.0.0"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
